from fastapi.middleware.cors import CORSMiddleware
from ..config import settings

# CORS Middleware setup will be done in main.py, this file just defines the logic if needed
# For now, it will be kept minimal.
